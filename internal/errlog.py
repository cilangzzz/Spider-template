def ErrLog(moduleName, filePath, msg, err, todo):
    """
    错误日记
    :param todo:
    :param filePath:
    :param moduleName:
    :param msg:
    :param err:
    :return:
    """
    print(moduleName, filePath, msg, err, todo)
